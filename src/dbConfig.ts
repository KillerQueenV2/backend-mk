const password = 'mongo123'
const host = 'mkdb.25zpj.mongodb.net'
const connection = `mongodb+srv://mongomk:${password}@${host}/myFirstDatabase?retryWrites=true&w=majority`

export default connection
